<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.publicidentityprovider">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PublicIdentityProvider"
        android:usesCleartextTraffic="true">
        <activity android:name=".src.AppAuthorizationActivity"></activity>
        <activity android:name=".src.AllowedListActivity" />
        <activity android:name=".AllowedListActivity" />
        <activity android:name=".src.WaitingListActivity" />
        <activity android:name=".src.UserHomeActivity" />
        <activity android:name=".src.SignUpActivity" />
        <activity android:name=".src.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>